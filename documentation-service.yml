server:
  port: 10010

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

   
zuul:
  routes:
    campus:
      path: /campus/**
      serviceId: campus-service
    employee:
      path: /employee/**
      serviceId: employee-service